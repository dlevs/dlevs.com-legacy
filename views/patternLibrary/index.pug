extends ../layouts/main

block meta
    title=`${title} | Daniel Levett`
    meta(name="robots" content="noindex")

block content
    h1 Pattern Library
    ul
        li
            a(href=`${path}/typography`) Typography
        li
            a(href=`${path}/images`) Images
